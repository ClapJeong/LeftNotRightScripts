namespace LR.UI.Enum
{
  public enum RootType
  {
    Overlay,
    Popup,
    SceneLoading,
    SpeedRunTimer,
  }

  public enum ViewEvent
  {
    OnShowing,
    OnShowen,

    OnHiding,
    OnHidden,
  }

  public enum VisibleState
  {
    None,

    Showing,
    Showen,

    Hiding,
    Hidden,
  }
}
